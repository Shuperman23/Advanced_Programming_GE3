@model IEnumerable<CGASTOSFE.DTOs.ProductoDto>
@{
    ViewData["Title"] = "Productos";
}

<h1>Productos</h1>

<p>
    
    <a class="btn btn-primary" asp-action="Create">Ingresar producto</a>
</p>

<table class="table">
    <thead class="table-dark">
        <tr>
            <th>ID</th>
            <th>Nombre</th>
            <th>ProveedroId</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var autor in Model)
        {
            <tr>

                <td>@autor.Id</td>
                <td>@autor.Nombre</td>
                <td>@autor.ProveedorId</td>
                <td>
                    <a class="btn btn-info btn-sm" asp-action="Details" asp-route-id="@autor.Id">Detalles</a>
                    <a class="btn btn-warning btn-sm" asp-action="Edit" asp-route-id="@autor.Id">Editar</a>
                    <a class="btn btn-danger btn-sm" asp-action="Delete" asp-route-id="@autor.Id">Eliminar</a>
                </td>
            </tr>
        }
    </tbody>
</table>